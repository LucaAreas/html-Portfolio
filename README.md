# html-Portfolio (study fork)

Forked for study purposes — exploring how the original project structures its HTML/CSS.

This fork includes annotated fixes covering semantic HTML, accessibility (`alt` attributes, heading hierarchy), responsiveness (viewport meta tag, media queries), and CSS best practices (custom properties, reduced repetition, hover states).

Not intended for production use. All credit for the original concept and layout goes to the original author.
